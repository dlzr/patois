<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.undef.patois"
    android:versionCode="1"
    android:versionName="1.0">

    <application android:label="@string/app_name">

        <activity android:name=".PatoisMainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".EditLanguagesActivity" />

        <activity android:name=".BrowseWordsActivity" />

        <activity android:name=".EditWordActivity" />

        <activity android:name=".PracticeActivity" />

    </application>
</manifest> 
